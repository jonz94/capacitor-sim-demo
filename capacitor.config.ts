import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.jonz94.capacitorjs.demo.sim',
  appName: 'Sim Demo',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
